; Pool overrides (подключается после www.conf за счёт имени zz-*.conf).
; Для pm=dynamic обязательно:
;   min_spare_servers ≤ start_servers ≤ max_spare_servers ≤ max_children
[www]
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
