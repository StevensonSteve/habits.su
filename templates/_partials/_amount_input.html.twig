<input type="number" 
       name="amount" 
       placeholder="{{ placeholder|default('значение') }}" 
       class="{{ input_class }}" 
       step="any" 
       value="" 
       required
       {% if activity.unit == 2 %}
           min="0.01"
           {# step="0.01" вместо этого сделано отображение flash message error #}
       {% endif %}   
>
